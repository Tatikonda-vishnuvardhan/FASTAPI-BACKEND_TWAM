# MDM module has no own database table.
# It aggregates read-only GET endpoints that delegate to individual module repositories:
#   country, state, fabric, size, brand (+ BrandProductCount), cupsize, shippingtype, taxhsncode
